PA Workbench migration backup.
Contents: source code, config, database, key/token config templates, static assets, packaging config.
NOTE: .git is NOT included. If you need git push on the new machine, re-run setup-token.bat to configure the GitHub token.
